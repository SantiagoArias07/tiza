# URL of the Tiza backend API (the Render service).
# Local dev: leave empty or use http://localhost:4000
# Production (Vercel): set to your Render URL, e.g. https://tiza-server.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:4000
